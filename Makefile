install:
	pkg ins -y screen 
	cp bsi /usr/local/bin
	chmod +x /usr/local/bin/bsi
	cp bsistart /usr/local/bin
	chmod +x /usr/local/bin/bsistart
	cp bsistop /usr/local/bin
	chmod +x /usr/local/bin/bsistop
